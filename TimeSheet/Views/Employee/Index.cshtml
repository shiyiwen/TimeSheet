@model IEnumerable<TimeSheet.Models.tblEmployee>

@{
    ViewBag.Title = "Index";
}
@RenderPage("~/Shared/_Header.cshtml")
@RenderPage("~/Shared/_Navigation.cshtml")

<script type="text/javascript">
    $(document).ready(function () {
        $("table").tablesorter({
            headers: {
                3: {
                    sorter: false
                }
            }
        });

        $("#SearchFirstName").keyup(function () {
            var rows = $("#tblEmployeeBody").find("tr").hide();
            if (this.value.length) {
                var data = $.trim(this.value).split(" ");
                $.each(data, function (i, v) {
                    $("#tblEmployeeBody td.col1").filter(function () {
                        return $(this).text().toLowerCase().indexOf(v.toLowerCase()) > -1;
                    }).parent('tr').show();
                    $("#tblEmployeeBody td.col1").filter(function () {
                        return $(this).text().toLowerCase().indexOf(v.toLowerCase()) > -1;
                    }).parent('tr').addClass("showrow");
                });
                $("#SearchLastName").attr('disabled', true);
                $("#SearchEmail").attr('disabled', true);
            }
            else {
                rows.show();
                $("#SearchLastName").attr('disabled', false);
                $("#SearchEmail").attr('disabled', false);
            }
        })

        $("#SearchLastName").keyup(function () {
            var rows = $("#tblEmployeeBody").find("tr").hide();
            if (this.value.length) {
                var data = $.trim(this.value).split(" ");
                $.each(data, function (i, v) {
                    $("#tblEmployeeBody td.col2").filter(function () {
                        return $(this).text().toLowerCase().indexOf(v.toLowerCase()) > -1;
                    }).parent('tr').show();
                    $("#tblEmployeeBody td.col2").filter(function () {
                        return $(this).text().toLowerCase().indexOf(v.toLowerCase()) > -1;
                    }).parent('tr').addClass("showrow");
                });
                $("#SearchFirstName").attr('disabled', true);
                $("#SearchEmail").attr('disabled', true);
            }
            else {
                rows.show();
                $("#SearchFirstName").attr('disabled', false);
                $("#SearchEmail").attr('disabled', false);
            }
        })

        $("#SearchEmail").keyup(function () {
            var rows = $("#tblEmployeeBody").find("tr").hide();
            if (this.value.length) {
                var data = $.trim(this.value).split(" ");
                $.each(data, function (i, v) {
                    $("#tblEmployeeBody td.col3").filter(function () {
                        return $(this).text().toLowerCase().indexOf(v.toLowerCase()) > -1;
                    }).parent('tr').show();
                    $("#tblEmployeeBody td.col3").filter(function () {
                        return $(this).text().toLowerCase().indexOf(v.toLowerCase()) > -1;
                    }).parent('tr').addClass("showrow");
                });
                $("#SearchFirstName").attr('disabled', true);
                $("#SearchLastName").attr('disabled', true);
            }
            else {
                rows.show();
                $("#SearchFirstName").attr('disabled', false);
                $("#SearchLastName").attr('disabled', false);
            }
        })

        $("#ResetEmployeeSearch").click(function () {
            $('#SearchFirstName').val('');
            $('#SearchLastName').val('');
            $('#SearchEmail').val('');
            $("#SearchFirstName").attr('disabled', false);
            $("#SearchLastName").attr('disabled', false);
            $("#SearchEmail").attr('disabled', false);
            var rows = $("#tblEmployee").find("tr").show();
        })
    });
</script>

<div class="container">
    <h2>Employee</h2>
    <p align="center">
        @Html.ActionLink("Create New", "Create", null, new { @class = "button" })
    </p>
    <br>
    <div class="employee">
        <table id="tblEmployee">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.FirstName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.LastName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Email)
                    </th>
                    <th> </th>
                </tr>
                <tr>
                    <td>
                        <input id="SearchFirstName" type="text" placeholder="First Name" class="textbox" />
                    </td>
                    <td>
                        <input id="SearchLastName" type="text" placeholder="Last Name" class="textbox" />
                    </td>
                    <td>
                        <input id="SearchEmail" type="text" placeholder="Email" class="textbox" />
                    </td>
                    <td>
                        <input id="ResetEmployeeSearch" type="button" value="Reset" class="button" />
                    </td>
                </tr>
            </thead>

            <tbody id="tblEmployeeBody">
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="col1">
                            @Html.DisplayFor(modelItem => item.FirstName)
                        </td>
                        <td class="col2">
                            @Html.DisplayFor(modelItem => item.LastName)
                        </td>
                        <td class="col3">
                            @Html.DisplayFor(modelItem => item.Email)
                        </td>
                        @if (!item.del)
                        {
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.EmpID }, new { @class = "tblactionlink" }) |
                                @Html.ActionLink("Details", "Details", new { id = item.EmpID }, new { @class = "tblactionlink" }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.EmpID }, new { @class = "tblactionlink" })
                            </td>
                        }
                        else
                        {
                            <td>
                                @Html.ActionLink("Details", "Details", new { id = item.EmpID }, new { @class = "tblactionlink" })
                            </td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
@RenderPage("~/Shared/_Footer.cshtml")