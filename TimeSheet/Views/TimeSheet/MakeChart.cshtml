@{
    ViewBag.Title = "MakeChart";
}


@{
    var myChart = new Chart(width: 600, height: 400)
       .AddTitle("Employees")
       .AddSeries(chartType: "area",
          xValue: ViewBag.xline,
          yValues: ViewBag.yline)
       .Write();
}

